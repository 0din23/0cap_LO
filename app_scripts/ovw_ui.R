# Eventdriven UI ---------------------------------------------------------------
ovw_ui <- function() {
  sidebarLayout(
    sidebarPanel = sidebarPanel(
    ),
    mainPanel = mainPanel(
      width = 10,
      tabsetPanel(
        tabPanel("Skew Trader",

        )
      )
    )
  )
}